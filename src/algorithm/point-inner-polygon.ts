/**
 * @description judge point is inner the Polygon
 */

import { IPoint, IPointList } from '@/lib/interface';

export default (p: IPoint | Point, polygon: IPointList | Polugon): boolean => {
    return true;
};
